import { InjectModel } from '@nestjs/mongoose';
import { {{ pascalCase name }} } from 'interfaces';
import { MongoMapper } from 'mappers';
import { {{ pascalCase name }}Repository } from 'modules/persistence/{{ dashCase name }}/{{ dashCase name }}.repository';
import { Model } from 'mongoose';
import { MongoRepositoryImpl } from 'repositories';
import { {{ pascalCase name }}Entity } from 'schemas';

export class {{ pascalCase name }}RepositoryImpl
  extends MongoRepositoryImpl<{{ pascalCase name }}Entity, {{ pascalCase name }}>
  implements {{ pascalCase name }}Repository
{
  constructor(@InjectModel({{ pascalCase name }}Entity.name) protected readonly model: Model<{{ pascalCase name }}Entity>) {
    super(model, new MongoMapper());
  }
}
