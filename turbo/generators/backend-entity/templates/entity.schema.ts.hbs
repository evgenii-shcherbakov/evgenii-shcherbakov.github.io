import { Schema, SchemaFactory } from '@nestjs/mongoose';
import { DatabaseCollection } from '@packages/common';
import { {{ pascalCase name }} } from 'interfaces';
import { MongoEntity } from 'schemas/abstract/mongo.entity';

@Schema({ collection: DatabaseCollection.{{ collection }}, timestamps: true })
export class {{ pascalCase name }}Entity extends MongoEntity implements {{ pascalCase name }} {}

export const {{ pascalCase name }}Schema = SchemaFactory.createForClass({{ pascalCase name }}Entity);
