const server = import('{{ path }}');

export const config = {
  runtime: 'nodejs',
};

try {
  console.log(server);
  console.log(server.app);
} catch (e) {
  console.log(`Can't check`);
}

export default server.app;
