{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": [
        "src/**/*.ts",
        ".eslintrc.js",
        "Dockerfile",
        "nest-cli.json",
        "package.json",
        "tsconfig*.json"
      ],
      "outputs": ["dist/**"]
    }
  }
}
