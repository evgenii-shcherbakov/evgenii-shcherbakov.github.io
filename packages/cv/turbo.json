{
  "extends": ["//"],
  "pipeline": {
    "dev": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist/**"]
    },
    "build": {
      "inputs": ["src/**/*.ts"],
      "outputs": ["dist/**"]
    }
  }
}
