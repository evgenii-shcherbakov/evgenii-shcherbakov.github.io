import { CvLinkDirective } from './cv-link.directive';

describe('CvLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new CvLinkDirective();
    expect(directive).toBeTruthy();
  });
});
