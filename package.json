{
  "name": "evgenii-shcherbakov.github.io",
  "private": true,
  "author": "Evgenii Shcherbakov <iipekolict@gmail.com>",
  "license": "MIT",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "format": "turbo run format",
    "gen:package": "turbo gen package && yarn",
    "reset": "rm -rf .turbo && turbo run reset",
    "docker": "docker-compose -f docker-compose.yml -p platform up --build"
  },
  "devDependencies": {
    "@turbo/gen": "^1.13.2",
    "prettier": "^3.2.5",
    "turbo": "^1.13.2",
    "yarn": "^1.22.22"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
