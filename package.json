{
  "name": "evgenii-shcherbakov.github.io",
  "private": true,
  "author": "Evgenii Shcherbakov <iipekolict@gmail.com>",
  "license": "MIT",
  "scripts": {
    "format": "turbo run format",
    "dev:admin": "turbo run dev --filter=admin...",
    "dev:backend": "turbo run dev --filter=backend...",
    "dev:frontend": "turbo run dev --filter=frontend^... & cd apps/frontend && npm run dev",
    "dev": "turbo run dev",
    "build:admin": "turbo run build --filter=admin",
    "build:backend": "turbo run build --filter=backend",
    "build:frontend": "turbo run build --filter=frontend",
    "build": "turbo run build",
    "gen:package": "turbo gen package && npm install",
    "gen:api": "turbo gen api",
    "deploy:environment": "turbo run deploy:environment --filter=deploy"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.9",
    "prettier": "^3.2.4",
    "turbo": "^1.12.2"
  },
  "engines": {
    "node": "18.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
