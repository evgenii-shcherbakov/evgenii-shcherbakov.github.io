import { UserCollection } from '@admin/collections/core/user/user.collection';
import { AuthTokenCollection } from '@admin/collections/core/auth-token/auth-token.collection';

export const CORE_COLLECTIONS = [UserCollection, AuthTokenCollection];
